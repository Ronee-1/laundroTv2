{
  "name": "laundrot",
  "version": "2.0.0",
  "private": true,
  "description": "Multi-branch laundry management system",
  "scripts": {
    "dev": "cd spokes/branch-app && npm run dev",
    "build": "cd spokes/branch-app && npm install --legacy-peer-deps && npm run build",
    "build:api": "cd api && npm install && npx prisma generate",
    "build:all": "npm run build:api && npm run build",
    "typecheck": "cd spokes/branch-app && npm run typecheck",
    "prisma:generate": "cd api && npx prisma generate",
    "prisma:migrate": "cd api && npx prisma migrate deploy",
    "prisma:studio": "cd api && npx prisma studio"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
