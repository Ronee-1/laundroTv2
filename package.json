{
  "name": "laundrot",
  "version": "2.0.0",
  "private": true,
  "description": "Multi-branch laundry management system",
  "scripts": {
    "dev": "cd spokes/branch-app && npm run dev",
    "build": "cd spokes/branch-app && npm install --legacy-peer-deps && npm run build",
    "build:prisma": "cd api && npx prisma generate",
    "typecheck": "cd spokes/branch-app && npm run typecheck"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
